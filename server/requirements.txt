# Python dependencies for Claude Agent SDK API Server

# FastAPI server dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Agent orchestration
# agentpool is installed separately by setup.py (pip install -e ../agentpool)

# Scheduler dependencies
croniter>=2.0.1

# HTTP client for callbacks
aiohttp>=3.9.1
httpx>=0.25.2

# Channel plugins (optional — install for the channels you want)
python-telegram-bot>=21.0  # Telegram channel
slack-bolt>=1.18.0         # Slack channel

# Development and testing
pytest>=7.4.3
pytest-asyncio>=0.21.1

# System monitoring
psutil>=5.9.0  # For dashboard system stats
