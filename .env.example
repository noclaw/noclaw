# Personal Assistant - Environment Configuration
# Copy this file to .env and fill in your credentials

# ==== AUTHENTICATION (Required - Choose One) ====

# Option 1: Claude Code OAuth Token
# Get your token by running: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here

# Option 2: Anthropic API Key (for direct API access)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ==== WEBHOOK AUTHENTICATION ====

# API key for webhook endpoints (optional - unset allows all requests)
# NOCLAW_API_KEY=your-secret-key-here

# ==== SERVER CONFIGURATION ====

# Port configuration (default: 3000)
PORT=3000

# Data directory for SQLite and workspaces (default: data)
DATA_DIR=data

# ==== CONTAINER CONFIGURATION ====

# Docker image name (default: noclaw-worker:latest)
WORKER_IMAGE=noclaw-worker:latest

# Container resource limits
CONTAINER_TIMEOUT=120        # seconds
CONTAINER_MEMORY_LIMIT=512m  # memory limit
CONTAINER_CPU_LIMIT=1.0      # CPU cores

# ==== DEVELOPMENT SETTINGS ====

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Force mock mode (for testing without Docker/API)
# MOCK_MODE=true